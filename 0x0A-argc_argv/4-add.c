#include<stdio.h>
/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
int n, i, num, sum = 0;
i = 0;
while (i < n)
{
if (num >= 0)
{
sum = sum + num;
}
else
{
printf("Error");
}
i++;
}
printf("%d", sum);
return (0);
}
