#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * _strdup -  returns a pointer to a new
 * string which is a duplicate of the string str
 * @str: the new string is
 * Return: NULL if str = NULL
*/
char *_strdup(char *str)
{
return (0);
}
