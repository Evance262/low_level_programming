#include <stdio.h>
/**
 * main - function-like macro ABS(x) that
 * computes the absolute value of a number x
 *
 * Return: Always 0.
 */
#define ABS(x)

int abs_value()
{
    ABS();
    return (0);
}